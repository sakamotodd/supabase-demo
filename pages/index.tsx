import { BadgeCheckIcon } from '@heroicons/react/solid';
import type { NextPage } from 'next';
import Head from 'next/head';
import { Layout } from '../layout/Layout';


const Home: NextPage = () => {
  return (
    <Layout title='Auth'>{'test' }</Layout>
  );
};

export default Home;
